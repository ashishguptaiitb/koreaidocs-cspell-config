// cSpell Settings
{
  "enabledFileTypes": [
    "!txt",
    "!json",
    "!svg",
    "markdown",
    "!png"
  ],

  "version": "0.2",
  "language": "en",

  // "import": ["./config/kore-custom-rejected-words.json"],
  // Temporarily ignore the rejected words.
  
  "dictionaryDefinitions": [
    {
      "name": "allowedWords",
      "path": "./config/kore-custom-dict.txt",
      "addWords": true
    }
    // Do NOT define a "flaggedWords" dictionary below; it would allow those words.
  ],

  "dictionaries": [
    "allowedWords"
  ]
}
